import type { SVGProps } from "react";

// Ícone decorativo de prédio (multi-tenant). Aceita as props nativas de <svg>
// para permitir ajustes de tamanho/cor no ponto de uso.
export type TenantIconProps = SVGProps<SVGSVGElement>;
